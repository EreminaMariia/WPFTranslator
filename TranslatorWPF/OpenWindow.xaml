<Window x:Class="TranslatorWPF.OpenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TranslatorWPF"
        mc:Ignorable="d"
        Title="OpenWindow" Height="250" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 20 0 0" HorizontalAlignment="Center">

            <TextBox Name="pathTextBox" Width="250"/>
            <Button IsDefault="True" Content="Open" MaxWidth="50" Margin="0 0 0 0" Click="OpenButton_Click" />
            <Button IsCancel="True" MinWidth="60">Cancel</Button>

        </StackPanel>
    </Grid>
</Window>
